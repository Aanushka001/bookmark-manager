import { CollectionConfig } from "payload";

const Bookmarks: CollectionConfig = {
  slug: "bookmarks",
  fields: [
    {
      name: "title",
      type: "text",
      required: true,
    },
    {
      name: "url",
      type: "text",
      required: true,
    },
    {
      name: "tags",
      type: "array",
      fields: [
        {
          name: "tag",
          type: "text",
        },
      ],
    },
  ],
};

export default Bookmarks;
